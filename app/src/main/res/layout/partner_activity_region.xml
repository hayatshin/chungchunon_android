<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingVertical="25dp">

    <LinearLayout
        android:id="@+id/partnerRegionResultLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/region_box"
        android:orientation="vertical"
        android:paddingHorizontal="20dp"
        android:paddingBottom="30dp"
        app:layout_constraintBottom_toTopOf="@+id/partnerRegionViewPager"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/partnerRegionDescription"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="15dp"
            android:text="시/군/구 단위 거주지역을 목록에서 선택하세요."
            android:textSize="16sp"
            android:textStyle="bold" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/partnerRegionResult"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginRight="15dp"
                android:textSize="23sp"
                android:textStyle="bold"
                tools:text="서울특별시" />

            <TextView
                android:id="@+id/partnerSmallRegionResult"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="23sp"
                android:textStyle="bold"
                tools:text="광진구" />
        </LinearLayout>

    </LinearLayout>

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/partnerRegionViewPager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/partnerRegionRegisterBtn"
        app:layout_constraintTop_toBottomOf="@+id/partnerRegionResultLayout" />

    <androidx.appcompat.widget.AppCompatButton
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginTop="20dp"
        android:id="@+id/partnerRegionRegisterBtn"
        android:background="@drawable/default_button"
        android:layout_width="250dp"
        android:layout_height="50dp"
        android:text="앱 시작하기"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/partnerRegionViewPager" />

</androidx.constraintlayout.widget.ConstraintLayout>